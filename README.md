# libft
libft is a custom C library written from scratch that reimplements essential standard C functions and adds useful utilities. It focuses on memory management, string handling, file descriptor output, and linked list operations, and is built as a reusable static library for future C projects.
